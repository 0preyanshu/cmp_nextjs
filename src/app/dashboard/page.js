import React from 'react';
import DashboardPage from '@/components/dashboard/overview/dashboard-page';

const Overview = ({searchParams}) => {
  return <DashboardPage searchParams={searchParams} />;
};

export default Overview;
